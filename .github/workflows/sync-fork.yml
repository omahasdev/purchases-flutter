name: Sync Fork

on:
  schedule:
    # Runs daily at midnight UTC
    - cron: '0 0 * * *'
  workflow_dispatch:
    # Allow manual trigger from GitHub Actions UI

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add upstream remote
        run: git remote add upstream https://github.com/RevenueCat/purchases-flutter.git

      - name: Fetch upstream
        run: git fetch upstream

      - name: Merge upstream
        id: merge
        continue-on-error: true
        run: |
          git merge upstream/main --no-edit
          echo "merge_success=true" >> $GITHUB_OUTPUT

      - name: Handle merge conflict
        if: steps.merge.outputs.merge_success != 'true'
        run: |
          git merge --abort
          echo "::error::Merge failed due to conflicts. Manual intervention required."

      - name: Create issue on conflict
        if: steps.merge.outputs.merge_success != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const title = 'Fork sync failed: merge conflict detected';
            const body = `The automated fork sync workflow failed due to merge conflicts.

            **Action required**: Manually resolve conflicts and merge your fork.

            \`\`\`bash
            git fetch upstream
            git merge upstream/main
            # Resolve conflicts
            git add .
            git commit
            git push
            \`\`\`

            Workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`;

            // Check if issue already exists
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'sync-conflict'
            });

            if (issues.data.length === 0) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: title,
                body: body,
                labels: ['sync-conflict']
              });
            }

      - name: Push changes
        if: steps.merge.outputs.merge_success == 'true'
        run: git push origin main
